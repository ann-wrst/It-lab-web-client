import {React, Component} from 'react';

class Columns extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (<>Columns:</>)
    }
}

export default Columns;
